import { Injectable } from '@nestjs/common';

@Injectable()
export class StorageService {

    /*public async renameDirectory(srcDir: string, destDir: string) {
        try {
            renameSync(srcDir, destDir)
        } catch (error) {
            throw new InternalServerErrorException(error.message)
        }
    }*/

}
